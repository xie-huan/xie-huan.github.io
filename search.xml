<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>衡量线性回归算法好坏的指标——R-Squared</title>
    <url>/2020/04/17/R-squared/</url>
    <content><![CDATA[<h1 id="线性回归算法的一般评价指标"><a href="#线性回归算法的一般评价指标" class="headerlink" title="线性回归算法的一般评价指标"></a>线性回归算法的一般评价指标</h1><h2 id="MSE"><a href="#MSE" class="headerlink" title="MSE"></a>MSE</h2><p>待更新</p>
<h2 id="MAE"><a href="#MAE" class="headerlink" title="MAE"></a>MAE</h2><p>待更新</p>
<h2 id="RMSE"><a href="#RMSE" class="headerlink" title="RMSE"></a>RMSE</h2><p>待更新</p>
<a id="more"></a>
<h1 id="更具代表性的评价指标：R-Squared"><a href="#更具代表性的评价指标：R-Squared" class="headerlink" title="更具代表性的评价指标：R Squared"></a>更具代表性的评价指标：R Squared</h1><h2 id="为什么不使用前面的标准？"><a href="#为什么不使用前面的标准？" class="headerlink" title="为什么不使用前面的标准？"></a>为什么不使用前面的标准？</h2><p>前面介绍的<strong>MAE</strong>、<strong>MSE</strong>、<strong>RMSE</strong>算出来都是一个数值，用于大致估算由回归方程算出的值。</p>
<p>对于两种不同数据，如用使用同一种线性回归方法，用RMSE标准，预测房价的误差RMSE结果为5万元，预测成绩的差距RMSE为10分，我们只能用实际的感觉来估算这个误差大不大，不能用数据化的指标来判断线性回归算法是否足够好。</p>
<p>而kNN算法的衡量标准，是使用分类精确度来评判，值越接近1，表示分类效果越好；越接近0，表示分类效果越差。  </p>
<p>我们想让衡量线性回归算法的标准也具有上述性质，所以使用R Squared衡量标准。</p>
<h2 id="R-Squared的定义"><a href="#R-Squared的定义" class="headerlink" title="R Squared的定义"></a>R Squared的定义</h2><p><strong>公式:</strong>  <script type="math/tex">R^{2}=1-\frac{S S_{\text {residual}}}{S S_{\text {total}}}</script></p>
<p>其中：<br>$S S_{\text {residual}}$ = Residual Sum of Squares<br>$S S_{\text {total}}$ = Total Sum of Squares  </p>
<script type="math/tex; mode=display">R^{2}=1-\frac{\sum_{i}\left(\hat{y}^{(i)}-y^{(i)}\right)^{2}}{\sum_{i}\left(\bar{y}-y^{(i)}\right)^{2}}</script><h2 id="为什么要使用R-Squared？（或R-Squared为什么好？）"><a href="#为什么要使用R-Squared？（或R-Squared为什么好？）" class="headerlink" title="为什么要使用R Squared？（或R Squared为什么好？）"></a>为什么要使用R Squared？（或R Squared为什么好？）</h2><p>$S S_{\text {residual}}$可以看作使用我们模型预测产生的错误  </p>
<p>$S S_{\text {total}}$是使用$y=\bar{y}$预测产生的错误  </p>
<p>$y=\bar{y}$这个模型是y的均值，即所有预测值均为y的均值，与x无关，在机器学习中，这个模型称为Baseline Model。很显然这个模型的误差肯定很大。   </p>
<p>$\frac{S S_{\text {residual}}}{S S_{\text {total}}}$ 意味着我们的模型的误差和一个相对较差的模型的误差的比值，显然，我们的模型误差越小，这个比值就越小，而R Squared这个公式就越接近1，符合了上面所述的性质。</p>
<h2 id="R-Squared性质"><a href="#R-Squared性质" class="headerlink" title="R Squared性质"></a>R Squared性质</h2><ul>
<li>$R^{2}&lt;1$ </li>
<li>$R^{2}$ 越大越好。当我们的预测模型不犯任何错误时，$R^{2}$得到最大值1</li>
<li>当我们的模型等于基准模型时，$R^{2}$为0</li>
<li>如果$R^{2}&lt;0$，说明我们学习到的模型还不如基准模型。此时，很有可能我们的数据不存在任何线性关系。</li>
</ul>
<h2 id="公式变形"><a href="#公式变形" class="headerlink" title="公式变形"></a>公式变形</h2><p>$\begin{aligned} R^{2}=1-\frac{\sum_{i}\left(\hat{y}^{(i)}-y^{(i)}\right)^{2}}{\sum_{i}\left(\bar{y}-y^{(i)}\right)^{2}} &amp;=1-\frac{\left(\sum_{i=1}^{m}\left(\hat{y}^{(i)}-y^{(i)}\right)^{2}\right) / m}{\left(\sum_{i=1}^{m}\left(y^{(i)}-\bar{y}\right)^{2}\right) / m} &amp;=1-\frac{M S E(\hat{y}, y)}{\operatorname{Var}(y)} \end{aligned}$</p>
<h2 id="编程实现"><a href="#编程实现" class="headerlink" title="编程实现"></a>编程实现</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># R squared</span></span><br><span class="line"><span class="comment"># 加载数据</span></span><br><span class="line"><span class="keyword">from</span> sklearn <span class="keyword">import</span> datasets</span><br><span class="line">boston = datasets.load_boston()</span><br><span class="line">x = boston.data[:,<span class="number">5</span>]  <span class="comment"># 只使用房间数量这个特征</span></span><br><span class="line">y = boston.target</span><br><span class="line"></span><br><span class="line"><span class="comment"># 真实值中，数据统计有上限，去掉这种上限异常值</span></span><br><span class="line">x = x[y &lt; <span class="number">50</span>]</span><br><span class="line">y = y[y &lt; <span class="number">50</span>]</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 绘图</span></span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line">plt.scatter(x,y)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p><img src="/2020/04/17/R-squared/output_6_0.png" alt="房间个数与房价散点图"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 拆分数据为训练集、测试集</span></span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line">x_train,x_test,y_train,y_test = train_test_split(x,y,random_state=<span class="number">666</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用自己实现的简单线性回归</span></span><br><span class="line"><span class="keyword">from</span> Machine_Learning.LinearReg.SimpleLinearRegression <span class="keyword">import</span> SimpleLinearRegression2</span><br><span class="line">reg = SimpleLinearRegression2()</span><br><span class="line">reg.fit(x_train,y_train)</span><br><span class="line">y_predict = reg.predict(x_test)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Machine_Learning.LinearReg.metrics <span class="keyword">import</span> mean_squared_error</span><br><span class="line"><span class="keyword">from</span> Machine_Learning.LinearReg.metrics <span class="keyword">import</span> root_mean_squared_error</span><br><span class="line"><span class="keyword">from</span> Machine_Learning.LinearReg.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="comment"># 计算R squared</span></span><br><span class="line">R_squared = <span class="number">1</span> - mean_squared_error(y_test, y_predict) / np.var(y_test)</span><br><span class="line">R_squared</span><br></pre></td></tr></table></figure>
<pre><code>0.5682464825049472
</code></pre><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在自己的代码中实现后</span></span><br><span class="line"><span class="keyword">from</span> Machine_Learning.LinearReg.metrics <span class="keyword">import</span> r2_score</span><br><span class="line">r2_score(y_test, y_predict)</span><br></pre></td></tr></table></figure>
<pre><code>0.5682464825049472
</code></pre><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 调用sklearn中包实现</span></span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score</span><br><span class="line">r2_score(y_test,y_predict)</span><br></pre></td></tr></table></figure>
<pre><code>0.5682464825049474
</code></pre><h2 id="其中，也可调用在sklearn中的简单线性回归算法，不用自己实现的简单线性回归算法"><a href="#其中，也可调用在sklearn中的简单线性回归算法，不用自己实现的简单线性回归算法" class="headerlink" title="其中，也可调用在sklearn中的简单线性回归算法，不用自己实现的简单线性回归算法"></a>其中，也可调用在sklearn中的简单线性回归算法，不用自己实现的简单线性回归算法</h2>]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>线性回归</tag>
        <tag>评价指标</tag>
      </tags>
  </entry>
  <entry>
    <title>k近邻算法介绍及Python实现</title>
    <url>/2020/04/18/k%E8%BF%91%E9%82%BB%E7%AE%97%E6%B3%95%E4%BB%8B%E7%BB%8D%E5%8F%8APython%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="k近邻算法介绍"><a href="#k近邻算法介绍" class="headerlink" title="k近邻算法介绍"></a>k近邻算法介绍</h1><h2 id="什么是k近邻算法？"><a href="#什么是k近邻算法？" class="headerlink" title="什么是k近邻算法？"></a>什么是k近邻算法？</h2><p>　　k近邻算法，即k-Nearest Neighbor，简称<strong>kNN</strong>算法，后续内容中，我们称其为<strong>kNN</strong>算法。<strong>kNN</strong>算法适合解决分类问题，其中<strong>k</strong>表示距离测试样本最近的<strong>k</strong>个样本数据。找到这k个样本数据中数量最多的一类，作为最后分类的结果。</p>
<a id="more"></a>
<h2 id="kNN算法的特点"><a href="#kNN算法的特点" class="headerlink" title="kNN算法的特点"></a>kNN算法的特点</h2><p>　　kNN算法是典型的<strong>非参数</strong>学习算法，即在学习过程中无任何参数。很明显，在kNN算法中，数据即模型。kNN算法天然可以解决多回归问题。  </p>
<p>​        kNN算法思想简单，实现起来也不复杂，但通过实现具体算法代码，我们需要掌握的是常规机器学习算法实现及常规从<strong>原始数据</strong>到<strong>训练模型</strong>再到<strong>评价模型好坏</strong>的全流程。  </p>
<h2 id="kNN算法的流程"><a href="#kNN算法的流程" class="headerlink" title="kNN算法的流程"></a>kNN算法的流程</h2><ol>
<li>计算测试数据到训练数据的距离</li>
<li>将距离进行排序，并取出前k个训练数据</li>
<li>将k个训练数据对应到类别，计算每个类别的个数</li>
<li>找到最大个数的类别，作为算法的返回值</li>
</ol>
<h2 id="相关概念"><a href="#相关概念" class="headerlink" title="相关概念"></a>相关概念</h2><p>　　和kNN有关的一个重要概念是：<strong>超参数</strong>。所谓超参数，就是在学习算法运行之前需要决定的参数，与之对应的是模型参数，模型参数是指在算法学习过程中的参数。因为kNN算法只需在算法运行前，给定k值和数据，就可以进行分类预测，所以，kNN算法没有模型参数。</p>
<p>　　我们如何寻找合适的超参数呢？常用方法有：<strong>领域知识、经验积累、实验搜索</strong>，由于在初学阶段，不能使用前面两种方法，<strong>实验搜索</strong>便是更适合初学者的方法。</p>
<p>　　其实，kNN算法不止k值这一个超参数，这将在后面的实现中进一步讨论。</p>
<h1 id="kNN算法实现"><a href="#kNN算法实现" class="headerlink" title="kNN算法实现"></a>kNN算法实现</h1><h2 id="算法核心实现"><a href="#算法核心实现" class="headerlink" title="算法核心实现"></a>算法核心实现</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 变量定义：</span></span><br><span class="line"><span class="comment"># X_train, y_train: 分别表示训练数据的特征和标签，数据类型均为ndarray</span></span><br><span class="line"><span class="comment"># x：单个测试样本</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="comment"># 定义距离数组，使用列表解析计算测试样本到训练数据的距离</span></span><br><span class="line">distances = np.array([np.sqrt(np.sum((x_trian - x)**<span class="number">2</span>)) <span class="keyword">for</span> x_train <span class="keyword">in</span> X_train])</span><br><span class="line"><span class="comment"># 找到距离数组中值最小的k个值</span></span><br><span class="line">nearest = np.argsort(distances)[:k]</span><br><span class="line"><span class="comment"># 对应的k个样本类别</span></span><br><span class="line">top_k_y = [y_train[i] <span class="keyword">for</span> i <span class="keyword">in</span> nearest]</span><br><span class="line"><span class="comment"># 统计各个类别数量</span></span><br><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> Counter</span><br><span class="line">votes = Counter(top_k_y)</span><br><span class="line"><span class="comment"># 获得结果</span></span><br><span class="line">y_predict = votes.most_common(<span class="number">1</span>)[<span class="number">0</span>][<span class="number">0</span>]</span><br></pre></td></tr></table></figure>
<p>　　上述代码中，<code>x</code>即为我们需要预测的数据，<code>y_predict</code>即为算法给出的结果。</p>
<h2 id="封装kNN算法"><a href="#封装kNN算法" class="headerlink" title="封装kNN算法"></a>封装kNN算法</h2><p>　　2.1中介绍的算法，只适合<strong>单个测试</strong>数据的测试，显然，这样是很不方便我们调用的，所以将算法封装到一个类中，提供方便用户使用的接口，才是更合理的做法。用户只需要给接口提供<strong>训练数据</strong>和<strong>相应的k值</strong>（甚至k值也可以默认，不需要用户传递参数），然后再给出<strong>需要分类的测试数据</strong>，即可获得结果。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># kNN.py</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> Counter</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KNNClassifier</span>:</span></span><br><span class="line">	</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, k)</span>:</span></span><br><span class="line">        self.k = k</span><br><span class="line">        self._X_train = <span class="literal">None</span></span><br><span class="line">        self._y_train = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 训练模型，kNN中，数据即模型</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">fit</span><span class="params">(self, X_train, y_train)</span>:</span></span><br><span class="line">        <span class="keyword">assert</span> len(X_train) == len(y_train), <span class="string">"the length of X and y must be equal"</span></span><br><span class="line">        self._X_train = X_train</span><br><span class="line">        self._y_train = y_train</span><br><span class="line">        <span class="keyword">return</span> self</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 用模型进行预测，这里进行封装，可以一次预测多个数据。即用户接口</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">predict</span><span class="params">(self, X_predict)</span>:</span></span><br><span class="line">        <span class="keyword">assert</span> _X_train <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span> <span class="keyword">and</span> _y_train <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>, <span class="string">"must fit before predict!"</span></span><br><span class="line">        y_predict = [self._predict(x) <span class="keyword">for</span> x <span class="keyword">in</span> X_predict]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> np.array(y_predict)</span><br><span class="line">	</span><br><span class="line">    <span class="comment"># 上述2.1节核心代码</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_predict</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        distances = [np.sqrt(np.sum((x_train - x) ** <span class="number">2</span>)) <span class="keyword">for</span> x_train <span class="keyword">in</span> self._X_train]</span><br><span class="line">        topK_y = self._y_train[np.argsort(distances)[:self.k]]</span><br><span class="line">        votes = Counter(topK_y)</span><br><span class="line">        predict_y = votes.most_common(<span class="number">1</span>)[<span class="number">0</span>][<span class="number">0</span>]</span><br><span class="line">        <span class="keyword">return</span> predict_y</span><br></pre></td></tr></table></figure>
<h2 id="调用我们的kNN算法"><a href="#调用我们的kNN算法" class="headerlink" title="调用我们的kNN算法"></a>调用我们的kNN算法</h2><p>　　调用算法之前，需要准备好数据。我们使用封装在<code>sklearn</code>中的数据集：鸢尾花数据集。鸢尾花数据集封装在<code>sklearn.datasets</code>中，通过调用<code>load_iris()</code>函数即可加载数据。</p>
<p>　　加载完数据后，我们还需要做的是切分我们的数据集。将数据集划分为<strong>训练数据集</strong>和<strong>测试数据集</strong>，用训练数据集训练出模型，用测试数据集测试模型的好坏。<strong>如何划分数据集呢？</strong>我们可以自己实现随机划分的函数。但在这里，为了演示方便，使用<code>sklearn.model_selection</code>中的<code>train_test_split</code>函数，对数据集进行划分。在文章后面的调用中，我们将重复使用<strong>X_train、X_test、y_train、y_test</strong>这些数据。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn <span class="keyword">import</span> datasets</span><br><span class="line">iris = datasets.load_iris()</span><br><span class="line">X = iris.data	<span class="comment"># 特征矩阵</span></span><br><span class="line">y = iris.target	<span class="comment"># 标签矩阵，即类别</span></span><br><span class="line"><span class="comment"># train_test_split的参数详见官方文档，这里的test_size = 0.2指测试数据占总数据的20%</span></span><br><span class="line">X_train,X_test,y_train,y_test = train_test_split(X,y,test_size = <span class="number">0.2</span>)</span><br></pre></td></tr></table></figure>
<p>　　有了数据后，就可以很方便的调用我们自己实现的kNN算法了：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> .kNN <span class="keyword">import</span> KNNClassifier	<span class="comment"># 导入我们实现的kNN分类器</span></span><br><span class="line">kNN_classifier = KNNClassifier(<span class="number">4</span>)	<span class="comment"># k = 4</span></span><br><span class="line">kNN_classifier.fit(X_train,y_train)	<span class="comment"># 训练模型</span></span><br><span class="line">y_predict = kNN_classifier.predict(X_test)	<span class="comment"># 预测</span></span><br></pre></td></tr></table></figure>
<h2 id="评价我们的kNN算法"><a href="#评价我们的kNN算法" class="headerlink" title="评价我们的kNN算法"></a>评价我们的kNN算法</h2><p>　　上述2.3中的调用，很方便的得到了预测结果，但是，我们不知道我们预测的分类结果是否准确，或者说，我们不知道我们的kNN算法的模型是否具有很好的预测效果。为了评价学习模型，我们有<strong>分类精确度</strong>的概念，即在测试数据集上，我们预测正确的数量与测试数据集总量的比值。</p>
<p>　　在<code>sklearn</code>中，评价标准一般封装在<code>metrics.py</code>中。在我们的实现中，评价kNN算法的标准也创建在<code>metrics.py</code>中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># metrices.py</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">accuracy_score</span><span class="params">(y_true, y_predict)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> sum(y_predict == y_true) / len(y_true)</span><br></pre></td></tr></table></figure>
<p>　　同时，在kNN分类器中，也定义一个函数<code>score()</code>，可以直接通过测试数据集得到分类精确度。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># kNN.py</span></span><br><span class="line"><span class="keyword">from</span> .metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KNNClassifier</span>:</span></span><br><span class="line">    <span class="comment">#...</span></span><br><span class="line">    <span class="comment">#...</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">score</span><span class="params">(self, X_test, y_test)</span>:</span></span><br><span class="line">        y_predict = self.predict(X_test)</span><br><span class="line">        <span class="keyword">return</span> accuracy_score(y_test,y_predict)</span><br><span class="line">    <span class="comment">#...</span></span><br><span class="line">    <span class="comment">#...</span></span><br></pre></td></tr></table></figure>
<p>　　创建这个函数的目的是，用户可以根据是否需要知道预测值与否，来评价分类器的好坏。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="comment"># 先求得预测值，再计算准确度</span></span><br><span class="line"><span class="keyword">from</span> .metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line">y_predict = kNN_classifier.predict(X_test)</span><br><span class="line">accuracy_score(y_test,y_predict)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 或直接计算精确度</span></span><br><span class="line">kNN_classifier.score(X_test,y_test)</span><br></pre></td></tr></table></figure>
<h1 id="调用sklearn中的kNN算法"><a href="#调用sklearn中的kNN算法" class="headerlink" title="调用sklearn中的kNN算法"></a>调用sklearn中的kNN算法</h1><h2 id="直接简单调用"><a href="#直接简单调用" class="headerlink" title="直接简单调用"></a>直接简单调用</h2><p>　　我们自己的实现只完成了算法的核心部分，如想要更完全的功能，还是调用<code>sklearn</code>包更为方便。下面就简单使用<code>sklearn</code>中的kNN算法，来完成对数据的预测。我们还是使用上面例子中创建好的鸢尾花数据集进行实验。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> sklearn.neighbors <span class="keyword">import</span> KNeighborsClassifier	<span class="comment"># 调包</span></span><br><span class="line">classifier = KNeighborsClassifier(n_neighbors=<span class="number">4</span>)	<span class="comment"># 创建分类器，指定k=4</span></span><br><span class="line">classifier.fit(X_train,y_train)	<span class="comment"># 训练模型</span></span><br><span class="line">y_predict = classifier.predict(X_test)	<span class="comment"># 预测结果</span></span><br></pre></td></tr></table></figure>
<h2 id="调用sklearn中的评价标准"><a href="#调用sklearn中的评价标准" class="headerlink" title="调用sklearn中的评价标准"></a>调用sklearn中的评价标准</h2><p>　　    上面我们说到，在<code>sklearn</code>中，评价标准一般封装在<code>metrics.py</code>中，这里，我们尝试调用<code>sklearn</code>给我们提供的评价标准接口。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line">accuracy_score(y_test,y_predict)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 或不用知道预测结果，直接给出分类准确度</span></span><br><span class="line">classifier.score(X_test,y_test)</span><br></pre></td></tr></table></figure>
<h2 id="更多关于kNN的超参数"><a href="#更多关于kNN的超参数" class="headerlink" title="更多关于kNN的超参数"></a>更多关于kNN的超参数</h2><h3 id="有关距离的超参数"><a href="#有关距离的超参数" class="headerlink" title="有关距离的超参数"></a>有关距离的超参数</h3><h4 id="权重"><a href="#权重" class="headerlink" title="权重"></a>权重</h4><p>　　直到现在，我们讨论的各种实现，都只讨论了kNN算法的一个超参数：k。实际上，<code>sklearn</code>提供给我们的接口，不止这一个，其中另一个非常重要的超参数为是否考虑<strong>样本数据</strong>与<strong>测试样本数据</strong>之间的距离的<strong>权重</strong>。</p>
<p><img src="/2020/04/18/k%E8%BF%91%E9%82%BB%E7%AE%97%E6%B3%95%E4%BB%8B%E7%BB%8D%E5%8F%8APython%E5%AE%9E%E7%8E%B0/考虑距离.png" alt></p>
<p>　　上图中，<strong>绿色点</strong>代表待分类数据（<strong>测试数据</strong>），<strong>蓝色点</strong>和<strong>红色点</strong>为<strong>训练数据</strong>。如果令k为3，在不考虑距离的权重的情况下，测试数据将会被判定为<strong>蓝色点</strong>所在的类别。在考虑距离权重的情况下，测试数据离红色点更近，应考虑将测试数据分类到<strong>红色点</strong>所在的类别。</p>
<h4 id="距离定义"><a href="#距离定义" class="headerlink" title="距离定义"></a>距离定义</h4><p>　　在计算距离的时候，我们是否仔细想过距离的定义呢？在上述过程中，我们使用的距离为<strong>欧拉距离</strong>，但实际上，两个样本之间的距离计算方法却不止这一种。明可夫斯基（Minkowski）距离作为一种广泛的距离定义在<code>sklearn</code>中进行实现。什么是明可夫斯基距离呢？在这里我们无需知道其具体原理，下面给出公式即可：</p>
<script type="math/tex; mode=display">
\left(\sum_{i=1}^{n}\left|X_{i}^{(a)}-X_{i}^{(b)}\right|^{p}\right)^{\frac{1}{p}}</script><p>　　公式中的<code>p</code>即为kNN算法的又一个超参数，显然，只有当考虑距离<strong>权重</strong>时，给出<code>p</code>的值才有意义。当<code>p</code>为1的时候，即为曼哈顿距离，<code>p</code>为2的时候，即为欧拉距离。</p>
<p>　　其实，还有更多关于距离的定义。如向量空间余弦相似度、调整余弦相似度、皮尔森相关系数、Jaccard相似系数等。</p>
<h3 id="有关CPU核数与其他超参数"><a href="#有关CPU核数与其他超参数" class="headerlink" title="有关CPU核数与其他超参数"></a>有关CPU核数与其他超参数</h3><p>　　<code>n_jobs</code>是<code>sklearn</code>提供给我们的有关并行的超参数，它和计算机的核数相关。其中指定-1为使用所有核并行运行kNN算法。其他更多超参数详见官方文档：<a href="https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.KNeighborsClassifier.html?highlight=kneighborsclassifier#sklearn.neighbors.KNeighborsClassifier" target="_blank" rel="noopener">sklearn官方文档</a>。</p>
<h2 id="获取最佳超参数"><a href="#获取最佳超参数" class="headerlink" title="获取最佳超参数"></a>获取最佳超参数</h2><h3 id="为什么要获取最优超参数"><a href="#为什么要获取最优超参数" class="headerlink" title="为什么要获取最优超参数"></a>为什么要获取最优超参数</h3><p>　　当我们了解这些超参数后，寻找对应超参数的最优值是极为重要的。例如对于超参数k，如果我们选取的k值不恰当，如下图</p>
<p><img src="/2020/04/18/k%E8%BF%91%E9%82%BB%E7%AE%97%E6%B3%95%E4%BB%8B%E7%BB%8D%E5%8F%8APython%E5%AE%9E%E7%8E%B0/k小于类别数量.png" alt="k小于类别数量"></p>
<p>　　图中显示了k值取值不当时的一种情况：k小于等于类别的数量。导致绿色点距离最近的三个点为红色点、紫色点、蓝色点，若没有权重这一超参数，则此分类几乎无法进行下去，因为测试数据距离最近的三个点代表了三个类别！这可能导致分类准确度急剧下降！</p>
<h3 id="如何获取最佳超参数"><a href="#如何获取最佳超参数" class="headerlink" title="如何获取最佳超参数"></a>如何获取最佳超参数</h3><p>　　一种直观的解法是，对于每个可能的超参数的组合，每个组合中超参数可能的值，进行训练，给出分类准确度，找出分类准确度最大的超参数的组合及超参数的值。</p>
<p>　　下面给出获取超参数<strong>k</strong>的最佳值的过程：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> sklearn.neighbors <span class="keyword">import</span> KNeighborsClassifier</span><br><span class="line"><span class="comment"># 寻找最好的k值</span></span><br><span class="line">best_score = <span class="number">0</span></span><br><span class="line">best_k = <span class="number">-1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> k <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">11</span>):</span><br><span class="line">    knn_clf = KNeighborsClassifier(n_neighbors=k)</span><br><span class="line">    knn_clf.fit(X_train,y_train)</span><br><span class="line">    score = knn_clf.score(X_test,y_test)</span><br><span class="line">    <span class="keyword">if</span> score &gt; best_score:</span><br><span class="line">        best_k = k</span><br><span class="line">        best_score = score</span><br><span class="line">print(<span class="string">"best k: "</span> + str(best_k))</span><br><span class="line">print(<span class="string">"best score: "</span>+ str(best_score))</span><br></pre></td></tr></table></figure>
<p>　　上述代码中，我们将k设置为从1到10，训练模型（fit）并给出模型在测试数据集上的分类准确度（score），若当前k值计算出的准确度比原有的best_score大，则记录k值和score值。</p>
<p>　　这就是所谓的调参过程！</p>
<p>　　下面再给出超参数k和超参数考虑距离权重weights组合的最优超参数求解过程：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line">best_method = <span class="string">""</span></span><br><span class="line">best_score = <span class="number">0</span></span><br><span class="line">best_k = <span class="number">-1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># weights为uniform时，表示不考虑距离的权重，为distance时，表示考虑距离权重</span></span><br><span class="line"><span class="keyword">for</span> method <span class="keyword">in</span> [<span class="string">"uniform"</span>,<span class="string">"distance"</span>]:</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">11</span>):</span><br><span class="line">        knn_clf = KNeighborsClassifier(n_neighbors=k,weights=method)</span><br><span class="line">        knn_clf.fit(X_train,y_train)</span><br><span class="line">        score = knn_clf.score(X_test,y_test)</span><br><span class="line">        <span class="keyword">if</span> score &gt; best_score:</span><br><span class="line">            best_k = k</span><br><span class="line">            best_score = score</span><br><span class="line">            best_method = method</span><br><span class="line"></span><br><span class="line">print(best_k)</span><br><span class="line">print(best_method)</span><br><span class="line">print(best_score)</span><br></pre></td></tr></table></figure>
<h3 id="网格搜索"><a href="#网格搜索" class="headerlink" title="网格搜索"></a>网格搜索</h3><p>　　很显然，上述寻找最优超参数的过程很繁琐，需要人工来编写每一组可能的超参数。超参数之间还有各种限制，例如超参数p要在考虑距离权重的时候（即weights设为distance时），才有存在的意义。上述搜索过程称为网格搜索，网格搜索即为按照参数的各种组合，搜索出最优参数的组合。由于超参数之间存在依赖关系，则自己用循环来搭配进行网格搜索变得麻烦，<code>sklearn</code>提供了Grid Search网格搜索来简化搜索过程。<code>GridSearchCV</code>封装在<code>sklearn.model_selection</code>这个模块中。下面用代码简单演示一下搜索过程。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">from</span> sklearn.neighbors <span class="keyword">import</span> KNeighborsClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> GridSearchCV</span><br><span class="line"><span class="comment"># 定义超参数的组合</span></span><br><span class="line">param_grid = [</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">"weights"</span>:[<span class="string">"uniform"</span>],</span><br><span class="line">        <span class="string">"n_neighbors"</span>:[i <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">11</span>)]</span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">"weights"</span>:[<span class="string">"distance"</span>],</span><br><span class="line">        <span class="string">"n_neighbors"</span>:[i <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">11</span>)],</span><br><span class="line">        <span class="string">'p'</span>:[i <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">6</span>)]</span><br><span class="line">    &#125;</span><br><span class="line">]</span><br><span class="line"><span class="comment"># 创建默认分类器</span></span><br><span class="line">knn_clf = KNeighborsClassifier()</span><br><span class="line"><span class="comment"># 设置参数</span></span><br><span class="line">grid_search = GridSearchCV(knn_clf, param_grid) </span><br><span class="line"><span class="comment"># 搜索最佳超参数组合及各个参数值</span></span><br><span class="line">grid_search.fit(X_train,y_train)</span><br><span class="line"><span class="comment"># 获取搜索到的最佳分类器</span></span><br><span class="line">knn_clf = grid_search.best_estimator_</span><br><span class="line"><span class="comment"># 使用分类器预测</span></span><br><span class="line">knn_clf.predict(X_test)</span><br></pre></td></tr></table></figure>
<p>　　在运行完上面的代码后，实例对象<code>grid_search</code>中，就存储了最佳超参数的组合和各个参数的值。通过代码<code>print(grid_search.best_params_)</code>来打印出该值，通过代码<code>print(grid_search.best_score_)</code>来打印出该最佳超参数的组合对应的分类准确度。同时，可以很方便的通过代码<code>knn_clf = grid_search.best_estimator_</code>来获取最佳超参数对应的分类器！</p>
<h1 id="kNN中的数据处理"><a href="#kNN中的数据处理" class="headerlink" title="kNN中的数据处理"></a>kNN中的数据处理</h1><h2 id="为什么要进行数据处理"><a href="#为什么要进行数据处理" class="headerlink" title="为什么要进行数据处理"></a>为什么要进行数据处理</h2><h2 id="数据归一化方法"><a href="#数据归一化方法" class="headerlink" title="数据归一化方法"></a>数据归一化方法</h2><h3 id="最值归一化"><a href="#最值归一化" class="headerlink" title="最值归一化"></a>最值归一化</h3><h3 id="均值方差归一化"><a href="#均值方差归一化" class="headerlink" title="均值方差归一化"></a>均值方差归一化</h3><h2 id="归一化方法实现"><a href="#归一化方法实现" class="headerlink" title="归一化方法实现"></a>归一化方法实现</h2><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1>]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>kNN</tag>
        <tag>代码实现</tag>
      </tags>
  </entry>
</search>
